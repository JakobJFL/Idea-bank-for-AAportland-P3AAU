using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace IdeaBank.Data
{
    class test
    {
        public int Ting()
        {
            /*
            List<string> list = new()
            {
                "hej",
                "kat",
                "jakob"
            };
            IEnumerable<string> hej = list
                .Where(s => s.Length == 5);// bliver lavet til SQL så længe vi kommmunikerer med IQueryable interfacet

            context.Persons
                .Include(p => p.City)
                    .AlsoInclude(c => c.Coordinates)
                .Include(p => p.Payment);

            return list[0] switch
            {
                "hej" => 1,
                "kat" => 2,
                "jakob" => 3,
                _ => -1
            };
            */
            return 0;
        }
    }
}
